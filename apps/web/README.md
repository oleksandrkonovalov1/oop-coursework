# Веб-клієнт «Довідник абітурієнта»

SPA на React 19 + TypeScript + Vite + Tailwind CSS; працює з API бекенда (`apps/api`).

- Розробка: `pnpm --filter web dev` (з кореня репозиторію)
- Продакшн-збірка: `pnpm --filter web build` — результат у `dist/`, його роздає ASP.NET Core
